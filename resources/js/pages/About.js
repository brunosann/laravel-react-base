import React from "react";
import { useLocation } from "react-router-dom";

const About = () => {
  document.title = "Sobre";
  return (
    <main className="showEntry">
      <h1>Sobre</h1>
    </main>
  );
};

export default About;
